Review the entire codebase and perform the following tasks:

1. Identify and remove redundant, unused, or unnecessary code and CSS styles.
2. Ensure that all colors are defined and referenced exclusively from `variable.css`.
   - Do not use hardcoded color values anywhere in the codebase.
3. Review the application for performance and security issues.
   - Apply improvements where appropriate and safe.

Important constraints:
- Do NOT modify or review any `appsettings` files.
- Do NOT change application behavior unless required for performance or security improvements.
- Keep all changes minimal, clean, and well-justified.